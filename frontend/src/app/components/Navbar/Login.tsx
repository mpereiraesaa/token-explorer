'use client';
import React from 'react';
import { LoginButtonContainer } from '@/app/components/Navbar/';

export const LoginButton = () => {
  return (
    <LoginButtonContainer size='medium'>
      Login
    </LoginButtonContainer>
  );
};
